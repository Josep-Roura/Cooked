# Cooked-AI environment configuration
# Copy this file to .env.local and replace the placeholder values with your project credentials.

# Supabase REST endpoint for your project (https://project-ref.supabase.co)
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"

# Supabase service role key (keep this value secret and never expose it on the client)
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# OpenAI API key used for plan generation (optional until AI integration is enabled)
OPENAI_API_KEY="sk-your-openai-api-key"
